{
  "name": "waddleperf",
  "version": "1.0.0",
  "description": "WaddlePerf - Network Performance Testing Platform",
  "scripts": {
    "build": "cd website && npm install && npm run build",
    "dev": "cd website && npm run dev",
    "start": "cd website && npm run start"
  },
  "workspaces": [
    "website"
  ]
}