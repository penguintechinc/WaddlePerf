"""Model definitions for WaddlePerf Unified API.

NOTE: With penguin-dal, table schemas are discovered automatically via
SQLAlchemy MetaData.reflect() — these model definition files are retained
for reference only and are no longer used at runtime.
"""
